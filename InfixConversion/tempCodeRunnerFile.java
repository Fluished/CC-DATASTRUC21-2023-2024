            // for (int j = 0; j < solution.indexOf(String.valueOf(value)); j++) {
            // for (int j2 = 2; j2 > 0; j2--) {
            // solution.remove(solution.indexOf(String.valueOf(value)) - 1);
            // }

            // }